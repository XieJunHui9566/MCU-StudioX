<UserControl x:Class="StudioX.Desktop.GitGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StudioX.Desktop"
             FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="12">
 <UserControl.Resources>
  <Style x:Key="GitListItem" TargetType="ListBoxItem">
   <Setter Property="Foreground" Value="{DynamicResource Text}"/>
   <Setter Property="Background" Value="Transparent"/>
   <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
   <Setter Property="Padding" Value="0"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
    <Border x:Name="RowSurface" Background="{TemplateBinding Background}"><ContentPresenter/></Border>
    <ControlTemplate.Triggers>
     <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="{DynamicResource HoverSurface}"/></Trigger>
     <Trigger Property="IsSelected" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="{DynamicResource Panel}"/></Trigger>
    </ControlTemplate.Triggers>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
 </UserControl.Resources>
 <Grid Background="{DynamicResource Background}">
  <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition/><RowDefinition Height="25"/></Grid.RowDefinitions>
  <Border Grid.Row="0" Background="{DynamicResource ChromeSurface}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1">
   <DockPanel Margin="12,6">
    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
     <Button x:Name="LoadMoreButton" Content="更多" ToolTip="加载更多提交" Click="LoadMore_Click" Margin="3,0"/>
     <Button Content="仓库设置" ToolTip="设置当前工程的提交身份与远端" Click="Settings_Click" Margin="3,0"/>
     <Button Content="获取" ToolTip="从远端获取引用，不修改工作区" Click="Fetch_Click" Margin="3,0"/>
     <Button Content="拉取" ToolTip="仅快进拉取当前分支" Click="Pull_Click" Margin="3,0"/>
     <Button Content="推送" ToolTip="推送当前分支" Click="Push_Click" Margin="3,0"/>
     <Button ToolTip="刷新 Git 状态" Click="Refresh_Click" Style="{StaticResource ToolButton}" Margin="7,0,0,0"><local:Icon Kind="restart"/></Button>
    </StackPanel>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
     <local:Icon Kind="git-branch" Foreground="{DynamicResource Accent}" Margin="0,0,8,0"/>
     <TextBlock Text="Git 图谱" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,14,0"/>
     <TextBlock x:Name="CurrentBranchText" Text="未打开工程" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" MaxWidth="145" TextTrimming="CharacterEllipsis" Margin="0,0,12,0"/>
     <ComboBox x:Name="BranchFilter" Width="180" MinHeight="28" Height="28" SelectionChanged="Filter_Changed" ToolTip="筛选分支" Margin="0,0,8,0"/>
     <TextBox x:Name="SearchBox" Width="195" Height="29" TextChanged="Search_Changed" ToolTip="按主题、作者或提交号搜索"/>
     <TextBlock Text="搜索" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="7,0,0,0"/>
    </StackPanel>
   </DockPanel>
  </Border>
  <Grid Grid.Row="1">
   <Grid.ColumnDefinitions><ColumnDefinition Width="240" MinWidth="195"/><ColumnDefinition Width="4"/><ColumnDefinition Width="*" MinWidth="360"/><ColumnDefinition Width="4"/><ColumnDefinition Width="370" MinWidth="295"/></Grid.ColumnDefinitions>
   <Grid Grid.Column="0" Background="{DynamicResource ToolSurface}">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <StackPanel Margin="12,11,12,8">
     <TextBlock Text="工作区更改" FontSize="13" FontWeight="SemiBold"/>
     <TextBlock x:Name="WorkingSummary" Text="打开工程后显示更改" Foreground="{DynamicResource Muted}" Margin="0,4,0,0" TextWrapping="Wrap"/>
    </StackPanel>
    <ListBox x:Name="WorkingFilesList" Grid.Row="1" SelectionMode="Extended" SelectionChanged="WorkingFiles_SelectionChanged" ItemContainerStyle="{StaticResource GitListItem}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Auto">
     <ListBox.ItemTemplate><DataTemplate><Grid Height="31" Margin="10,0"><Grid.ColumnDefinitions><ColumnDefinition Width="26"/><ColumnDefinition/></Grid.ColumnDefinitions>
      <TextBlock Text="{Binding Status}" Foreground="{DynamicResource Accent}" FontFamily="Consolas" FontWeight="SemiBold" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="1" Text="{Binding Path}" ToolTip="{Binding Path}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
     </Grid></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
    <StackPanel Grid.Row="2" Margin="10,8,10,11">
     <WrapPanel><Button x:Name="StageButton" Content="暂存选中" Click="Stage_Click" Margin="0,0,5,0"/><Button x:Name="UnstageButton" Content="取消暂存" Click="Unstage_Click"/></WrapPanel>
     <TextBox x:Name="CommitMessage" Height="65" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,9,0,7" TextChanged="CommitMessage_Changed" ToolTip="提交说明；请先保存编辑器中的文件"/>
     <Button x:Name="CommitButton" Content="提交已暂存更改" Click="Commit_Click" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Stretch"/>
     <Border BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Margin="0,13,0,0"/>
     <DockPanel Margin="0,11,0,6"><TextBlock Text="分支与标签" FontWeight="SemiBold"/><Button DockPanel.Dock="Right" Content="新建" Style="{StaticResource LinkButton}" Click="CreateBranch_Click" HorizontalAlignment="Right" Margin="4,-6,0,-6"/></DockPanel>
    </StackPanel>
    <Grid Grid.Row="3"><Grid.RowDefinitions><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <ListBox x:Name="RefsList" ItemContainerStyle="{StaticResource GitListItem}" Background="Transparent" BorderThickness="0" MouseDoubleClick="Refs_DoubleClick" PreviewMouseRightButtonDown="Refs_RightButtonDown" SelectionChanged="Refs_SelectionChanged">
      <ListBox.ItemTemplate><DataTemplate><Grid Height="31" Margin="10,0"><Grid.ColumnDefinitions><ColumnDefinition Width="20"/><ColumnDefinition/></Grid.ColumnDefinitions>
       <TextBlock Text="{Binding Marker}" Foreground="{DynamicResource Accent}" FontWeight="Bold" VerticalAlignment="Center"/>
       <TextBlock Grid.Column="1" Text="{Binding Display}" ToolTip="{Binding Display}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
      </Grid></DataTemplate></ListBox.ItemTemplate>
     </ListBox>
     <WrapPanel Grid.Row="1" Margin="8,5,8,9"><Button x:Name="CheckoutButton" Content="切换" Click="Checkout_Click" Margin="2"/><Button x:Name="MergeButton" Content="合并" Click="Merge_Click" Margin="2"/><Button x:Name="DeleteBranchButton" Content="删除" Click="DeleteBranch_Click" Margin="2"/></WrapPanel>
    </Grid>
   </Grid>
   <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource Background}" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"/>
   <Grid Grid.Column="2">
    <Grid.RowDefinitions><RowDefinition Height="31"/><RowDefinition/></Grid.RowDefinitions>
    <Grid Grid.Row="0" Background="{DynamicResource ToolSurface}"><Grid.ColumnDefinitions><ColumnDefinition Width="126"/><ColumnDefinition Width="*"/><ColumnDefinition Width="106"/><ColumnDefinition Width="120"/><ColumnDefinition Width="78"/></Grid.ColumnDefinitions>
     <TextBlock Grid.Column="0" Text="图谱" Foreground="{DynamicResource Muted}" Margin="15,0,0,0" VerticalAlignment="Center"/>
     <TextBlock Grid.Column="1" Text="提交" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
     <TextBlock Grid.Column="2" Text="作者" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
     <TextBlock Grid.Column="3" Text="日期" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
     <TextBlock Grid.Column="4" Text="哈希" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
    </Grid>
    <ListBox x:Name="CommitList" Grid.Row="1" Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource GitListItem}" PreviewMouseLeftButtonDown="CommitList_LeftButtonDown" PreviewMouseRightButtonDown="CommitList_RightButtonDown" SelectionChanged="CommitList_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
     <ListBox.ItemTemplate><DataTemplate><Grid Height="38" MinWidth="570"><Grid.ColumnDefinitions><ColumnDefinition Width="126"/><ColumnDefinition Width="*"/><ColumnDefinition Width="106"/><ColumnDefinition Width="120"/><ColumnDefinition Width="78"/></Grid.ColumnDefinitions>
      <local:GitGraphGlyph Row="{Binding GraphRow}" Width="126" Height="38"/>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
       <TextBlock Text="{Binding Subject}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" MaxWidth="360" ToolTip="{Binding Subject}"/>
       <ItemsControl ItemsSource="{Binding Refs}" Margin="7,0,0,0"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate><DataTemplate><Border Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="3" Padding="4,1" Margin="2,0"><TextBlock Text="{Binding}" Foreground="{DynamicResource Accent}" FontSize="11"/></Border></DataTemplate></ItemsControl.ItemTemplate>
       </ItemsControl>
      </StackPanel>
      <TextBlock Grid.Column="2" Text="{Binding Author}" Foreground="{DynamicResource Muted}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Author}" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="3" Text="{Binding DateText}" Foreground="{DynamicResource Muted}" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="4" Text="{Binding ShortHash}" Foreground="{DynamicResource Muted}" FontFamily="Consolas" VerticalAlignment="Center"/>
     </Grid></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
   </Grid>
   <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="{DynamicResource Background}" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"/>
   <Grid Grid.Column="4" Background="{DynamicResource ToolSurface}">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="130"/><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
    <StackPanel Margin="13,12,13,10"><TextBlock Text="提交详情" FontSize="13" FontWeight="SemiBold"/><TextBlock x:Name="DetailTitle" Text="选择一条提交查看详情" FontSize="14" TextWrapping="Wrap" Margin="0,11,0,0"/><TextBlock x:Name="DetailMetadata" Foreground="{DynamicResource Muted}" TextWrapping="Wrap" Margin="0,8,0,0"/></StackPanel>
    <DockPanel Grid.Row="1" Margin="11,0,11,6"><TextBlock Text="更改的文件" FontWeight="SemiBold"/><Button x:Name="CompareButton" DockPanel.Dock="Right" Content="比较" Click="Compare_Click" Style="{StaticResource LinkButton}" ToolTip="先选中一条提交，再按住 Ctrl 选另一条提交"/></DockPanel>
    <ListBox x:Name="ChangedFilesList" Grid.Row="2" SelectionChanged="ChangedFiles_SelectionChanged" ItemContainerStyle="{StaticResource GitListItem}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Auto">
     <ListBox.ItemTemplate><DataTemplate><Grid Height="28" Margin="11,0"><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="{Binding Status}" Foreground="{DynamicResource Accent}" FontFamily="Consolas" VerticalAlignment="Center"/><TextBlock Grid.Column="1" Text="{Binding Path}" ToolTip="{Binding Path}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/></Grid></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
    <TextBlock Grid.Row="3" Text="差异" FontWeight="SemiBold" Margin="13,10,13,6"/>
    <TextBox x:Name="DiffBox" Grid.Row="4" IsReadOnly="True" Text="选择文件查看差异" FontFamily="Cascadia Mono, Consolas, Microsoft YaHei UI" FontSize="11" AcceptsReturn="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" BorderThickness="0" Padding="12,7"/>
   </Grid>
  </Grid>
  <Border Grid.Row="2" Background="{DynamicResource ChromeSurface}" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0"><TextBlock x:Name="GitStatus" Text="就绪" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="11,0" TextTrimming="CharacterEllipsis"/></Border>
 </Grid>
</UserControl>
