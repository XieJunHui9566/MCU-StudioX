# 桌面工作台 / 2026-09

本轮根据用户给出的 CLion 美术方向和 MounRiver 欢迎页参考重做界面。使用中性深灰、蓝色交互重点、绿色构建图标、紧凑间距和应用自绘矢量符号；应用主标识与操作图标使用原创资产，器件厂商商标仅用于识别对应厂商。

## 窗口布局

- 自定义标题栏和原生窗口缩放、菜单栏、常用操作工具栏。
- 顶部工具栏的「新建、打开、保存、编译、下载、调试、停止、外观」常驻中文标签与矢量图标；编译使用绿色代码构建符号，不使用 emoji。工程名称过长时省略显示，悬停可看全名。
- 最大化使用窗口所在屏幕的工作区，避免状态栏和日志末行落入任务栏后方。原生边界以物理像素计算，保留还原窗口和系统缩放行为，不通过固定底部留白补偿。
- 左侧工具条和可调整宽度的工程窗口；工程树按需读取真实目录，按 C、H、C++、汇编、链接脚本、CMake、JSON、XML/SVD 等类型显示矢量图标，文件夹区分展开/收起。
- 工程树与编辑标签的文件图标使用稍高饱和度的金色文件夹、蓝色 C、紫色 H 和三色 CMake，轻微增强填充；浅色主题仍降低亮度以保持对比。
- 中央文档区：欢迎页、多文件源码标签、器件与模板、串口与波形、扩展管理。标签都有 X 关闭按钮，较多时单行横向滚动，选中标签自动滚入视野。
- 底部构建/设备输出，可调整高度和隐藏。
- 欢迎页提供新建、打开、导包、最近工程、外观和工具入口。未打开过工程时显示真实空状态，不填充演示项目。
- 最近打开的工程保存在用户目录。切换文件保留未保存内容；关闭文件、切换工程或退出时检查对应文件的修改并询问保存。取消批量关闭保留全部内存文档，保存失败也保留原标签。
- 启动优先读取最近工程记录，不访问历史工程所在磁盘，也不遍历/校验器件仓库。进入新建/导入器件包页面时，才在后台读取轻量器件目录；创建工程前只完整校验选中的包。现有工程打开和首页加载不等待其它器件包。
- 「文件 → 关闭工程」、工程栏标题右侧 X 或 `Ctrl+Shift+W` 关闭当前工程并回到欢迎页。先确认全部未保存文件，取消或保存失败保留工程；确认后清空资源树、源码标签、构建输出、工程/器件/工具集信息及导航历史，停止当前工程的 clangd，禁用编译和下载。磁盘文件与最近工程记录保留。构建/下载等操作进行时不能关闭工程，应先停止或等待完成。
- 「新建工程」先执行相同的关闭流程，再显示空资源栏和重新开始的器件选择。取消关闭则停留在原工程。

窗口边界检查：`--preview-window-layout <output-directory>` 在隔离窗口中检查普通、最大化和还原状态，以及两种输出面板高度下的构建/设备日志末行，输出像素边界记录和截图。窗口会短暂出现在主屏；不打开用户工程或执行构建、下载。

启动性能回归：`dotnet run --project tools/StudioX.StartupValidation -c Release -- <用户数据目录> <新输出目录>`。真实用户数据只读；在新输出目录生成隔离包，检查目录读取、工程创建、SDK 修改/缺失/新增文件拦截、清单修改、过期选择和取消。

2026-09-21 本机采样：49 个已安装包版本，旧启动路径完整校验耗时 35,194 ms；7 条最近工程记录读取约 39–42 ms，轻量器件目录读取 147 ms。通过电脑操作正常退出并重新启动自包含预览程序，在启动调用后 1,548 ms 的首次界面观察中，最近工程已显示，状态为「就绪」。该时间包含启动及界面观察工具开销，属于本机重启采样，不代表所有机器或冷磁盘耗时。另已从界面验证新建页厂商选择、最近列表打开 F407 工程和代码提示就绪。

主要快捷键：Ctrl+N 新建、Ctrl+O 打开、Ctrl+S 保存、Ctrl+Shift+S 全部保存、Ctrl+W 关闭当前标签、Ctrl+Tab / Ctrl+Shift+Tab 切换文件、Ctrl+F 代码内查找、Ctrl+Z/Y 撤销/重做、F5 刷新工程树（树获得焦点时）、Enter 打开树中选定文件、F7 构建、Alt+1 工程窗口、Alt+4 底部工具窗口、Ctrl+Alt+S 外观与背景。标签支持鼠标中键关闭；欢迎页与工具页关闭后可从菜单重新打开。

## 新建工程

新建工程列表对同一包 ID 显示已安装的最新版本，并显示版本号；较旧版本保留供已有工程追溯。导入旧版本不会让新建工程降级。

新建工程按「器件厂商 → 器件包 → 芯片型号 → 起始模板」逐级选择。厂商来自已安装包的 `vendor`，未选厂商时不展示器件包；切换厂商清空包、型号、搜索和模板，后续选项随选择启用。导入包后自动定位到对应厂商和包，仍由用户明确选择芯片和模板。已发布 STM32 包的 `STMicroelectronics / StudioX templates` 在选择器中统一显示为 `STMicroelectronics`，包文件本身保持不变；其他厂商按清单显示。

厂商选择框和下拉列表显示中文名，悬停显示英文名；已打开工程的信息页共用厂商展示信息。公开源码快照不包含厂商商标，缺少图像资源时显示厂商名称缩写；未知厂商保留器件包清单中的名称。

界面预览可用 `--preview-stm32 <output-directory> <STM32.mcupack> <AG32.mcupack>`，在隔离目录检查跨厂商筛选、选择清理和导入定位，并保存选择器截图。

## 编译与下载

构建日志末行显示「编译成功，退出代码：0」或「编译失败，退出代码：实际数值」；CMake 配置/构建和固件大小检查使用各自真实进程结果，超时另行标注。工具尚未启动、校验失败等无法获得退出码的异常显示「不可用」，原始诊断仍保留。内置下载流程在进入下载前也输出编译结论。

F7 保存修改并调用内置工具编译。带 `openOcd` 配置的器件包会启用工具栏下载按钮及「构建 → 下载固件…」。对话框列出该器件支持的烧录器、速度和序列号；用户点击「编译并下载」后才启动硬件流程。失败时显示原始 OpenOCD 诊断，不自动换协议、解锁或整片擦除。当前 STM32F407 提供 HAL/SPL/HAL+FreeRTOS/SPL+FreeRTOS 四种模板，描述中标明 8 MHz / 168 MHz 默认时钟。

## 工程树文件操作

- 右键首先选中鼠标下的文件/文件夹；空白区菜单以工程根目录为目标，也支持键盘菜单键。菜单提供打开/展开、新建文件、新建文件夹、复制、粘贴、重命名、复制完整路径、复制相对路径、在 Windows 资源管理器中显示、刷新。
- 新建或粘贴到选中文件夹；选中文件时使用其父目录。新建文件自动打开；刷新保留展开目录并定位结果。`Ctrl+C` / `Ctrl+V` 仅在树获得焦点时处理文件，编辑器中仍复制粘贴文本；`F2` 重命名，`Enter` 打开或展开/折叠，`F5` 刷新。
- 使用 Windows 文件剪贴板，可从系统资源管理器复制多个文件/文件夹。复制使用已保存的磁盘内容；粘贴始终创建副本，不移动来源。同名项使用「 - 副本」编号，绝不覆盖已有文件。批量复制先暂存，取消或失败清理本次新副本；支持二进制文件及空目录，拒绝链接和复制目录到它自身的子目录。
- 重命名支持文件、目录及仅改变大小写。已打开的子文件同步路径、标签、文件图标、语法类型和导航历史，保留原文档、未保存内容、撤销记录及光标；之后保存使用新路径。主目录或 `main.c` 改名后仍能重新打开工程。新建、粘贴、重命名完成后更新语言索引；不会自动修改 CMake 或 `#include` 中引用的路径。
- 工程根目录、`.studiox`、`.git`、器件清单及生成的内部 CMake 配置禁止在树内改名，其祖先目录也受保护；路径名称与同名冲突由应用服务检查。名称对话框跟随主题并提示非法名称。
- 检查入口：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-explorer <output-directory> <AG32-project-directory>`。只在输出目录的工程副本内操作，覆盖深浅菜单、重命名与保存、未保存文档/撤销/光标/导航、新建及副本定位、缺少原 `src/main.c` 时重新打开工程；不改写用户工程或剪贴板。

## 源代码编辑

- 鼠标位于编辑区时，`Ctrl+滚轮` 调整字号（10–28 px，每格 1 px），保留源码、光标和撤销记录，字号串行保存到用户偏好。普通滚轮仍滚动文档；高精度滚轮的小增量会累积。底部构建/设备输出也支持相同手势，日志字号仅作用于当前窗口。
- 全局 `WindowMouseWheel` 为主窗口及设置等子窗口统一接入滚轮，覆盖代码、日志、资源树、欢迎/新建页、器件列表和下拉列表。普通滚动和 Ctrl 缩放均优先读取 Windows `WM_MOUSEWHEEL` 自带的 `MK_CONTROL` 和鼠标位置，完成当前显示器 DPI 换算；窗口和 ScrollViewer 级 WPF 事件作为后备。滚动鼠标下方最近的可滚动容器，内层到边界后交给外层，只有横向滚动条的标签栏则横向滚动；无需先点击抢焦点，保留系统每格行数/整页设置及高精度滚轮累积。视图菜单还提供「编辑器字号 → 放大 / 缩小 / 恢复默认字号」。
- `--preview-project <output-directory> <project-directory>` 使用隔离工程副本，经窗口消息入口检查 Ctrl+滚轮导致实际文字行高变化、反向/高精度滚轮、普通滚轮和保存偏好，并检查关闭时取消/放弃/保存/保存冲突、清空工作区、语言服务重启与新建工程入口；不构建或访问硬件。

- 编辑区使用 AvalonEdit 6.3.1.120；C/C++、CMake、汇编、链接脚本、JSON、XML/SVD 随文件类型切换语法配色，支持深浅主题。注释、关键字、内置类型、函数形式的标识符、普通标识符、宏形式的常量、字符串、数字使用不同颜色。
- C/C++、CMake、链接脚本及 JSON 中成对的 `{}`、`()`、`[]` 按嵌套深度循环使用六种颜色，同一对保持同色，未配对括号保留原色；忽略注释、字符串、字符常量、C++ 原始字符串及 CMake 长括号字面量。配色随深浅主题切换，兼容字体柔光与编译错误波浪线。编辑后延迟 120 ms 在后台分析，切换文件丢弃旧结果，滚动只处理可见行，不修改源码或撤销记录。
- `--preview-brackets <output-directory> <project-directory>` 检查括号配对、字面量排除、编辑/撤销/快速切换、深浅主题的实际字形颜色；只读工程，不编译或连接硬件。
- 配色目前仍使用词法规则；代码提示、悬停及声明/定义跳转已独立接入 clangd 语义分析。编译诊断使用实际 GCC/CMake 构建输出，不使用 clangd 的推测结果。
- 编辑区上方提供「撤销」「重做」按钮，无记录或只读时禁用；`Ctrl+Z` 撤销，`Ctrl+Y` / `Ctrl+Shift+Z` 重做。每个文件保留独立撤销栈。
- `Ctrl+/` 切换选中行的注释（未选择时处理当前行），编辑菜单和右键菜单也有入口。C/C++ 使用 `//`，CMake 使用 `#`；保留缩进和空行，选区结束在下一行行首时不处理下一行。全部已注释时取消注释，否则整批加注释；整次操作可一次撤销。其他语言及只读文件不启用。
- 编译完成后，源文件中的错误显示红色波浪线，警告显示黄色波浪线；悬停优先显示编译器原因及文件、行列位置。支持工程内头文件、Windows/相对路径及 CMake 配置错误；无源文件行号的链接错误继续留在构建输出中。未打开文件的诊断在打开时显示，位置只绑定构建时的文本快照；编辑、改名、开始下一次构建或关闭工程清除旧诊断，编辑期间晚到的构建结果不套用到新文本。当前不做边输入边编译诊断。
- 验证入口：`--preview-editing <new-output> <F407-project>`，在隔离副本中执行真实编译错误及恢复，检查头文件/多标签、过期诊断、路径与列号、撤销重做和批量注释，不连接硬件。
- 行号、当前行背景、光标行列、文件路径、编码/换行信息和未保存标记可见。每个文件保留独立文档、撤销/重做历史、光标、选区和滚动位置；同一文件重复打开时定位到已有标签，同名文件显示所在目录。编辑区复用一个控件，不复制文档文本来切换。
- Ctrl+S 保存当前编辑文件；全部保存与构建前保存会处理所有已打开文件。保存绑定到文件和文本快照，异步期间切换标签不会写错文件。关闭后台文件不关闭当前文件，关闭最后一个文件回到欢迎页。
- 外观设置中选择本机已安装的等宽字体，默认 Cascadia Mono，回退 Consolas；字号 10–28 px。代码文字柔光默认开启，强度默认 80%，支持 0–100% 即时调节或关闭；浅色主题不应用柔光。使用单层贴边光晕：强度仅调整光晕亮度，对应 0–25% 不透明度；模糊半径按字号的 9% 计算并限制在 1–2 个 WPF 逻辑像素，字号缩放时同步调整。光晕单独绘制在可见文字下方并跟随语法颜色，原始字形、选区和光标不做模糊处理。偏好独立保存在用户目录 `editor.json`，保留已有开关及强度值；旧设置未包含强度时使用默认值。
- 文件服务位于 Application：支持 4 MiB 以内 UTF-8（有/无 BOM）及带 BOM 的 UTF-16，保存保留编码和现有换行；拒绝二进制和未支持编码。只读文件不可编辑，保存前检查磁盘哈希，发现外部修改时不覆盖。符号链接/重解析点显示为禁用项，工程树省略 `.git`。
- AvalonEdit 的 MIT 许可证随程序复制到 `licenses/AvalonEdit-MIT.txt`。字体采用系统安装字体，不附带第三方字体文件。

## 代码提示

- 内置 clangd 22.1.6，通过 LSP 读取当前内存文档，因此不必先保存或编译。支持 C/C++ 关键字、函数、变量、宏、类型、`.` / `->` 成员和头文件路径提示；不会自动添加 `#include` 或执行补全项携带的命令。
- 自动提示做 160 ms 防抖；`Ctrl+Space` 手动唤出，方向键选择，`Tab` / `Enter` 插入，`Esc` 关闭。函数补全补入括号，已有括号时不重复插入，修改可单步撤销。词中补全替换整个标识符，避免重复后缀。
- 输入 `(` / `,` / `)` 时显示或更新参数提示，支持嵌套调用；`Ctrl+Shift+Space` 手动唤出。菜单「编辑」提供同名入口。注释和普通字符串中抑制自动提示，`#include` 字符串支持路径提示。
- 编辑、移动光标、切换文件和关闭窗口会取消过期请求；响应只在相同文档版本和光标位置展示。子进程读写使用 UTF-8 字节长度分帧，文档位置使用 UTF-16；协议/组件故障显示状态并保留日志，不影响正常文本编辑。
- 分析参数来自当前工程的 `device/manifest.json`：精确器件、CPU/ABI、宏和包含目录。AG32 的 newlib 4.1.0 C 头文件以 `agrv-gcc-11.1.0` 标识随 IDE 分发；不会搜索 PATH 或启动编译器。当前未读取用户自行修改后的 CMake 配置，不等同于完整构建诊断。其他工具链的标准库头文件及完整 C++ 标准库需由相应发行工具集提供。
- 每个工程独立启动语言服务，退出时清理自有子进程。语言缓存位于用户数据目录，工程内不写入开发机绝对路径或临时编译数据库。语言服务准备与发行布局见 `runtime/README.md`。

## C/C++ 声明与定义导航

- 在符号上右键选择「转到定义」/「转到声明」，或按 `F12` / `Ctrl+F12`。函数、变量、宏、类型和寄存器成员使用语义位置；多个结果以文件与行列列表供选择，找不到时显示状态信息。
- 鼠标在符号上停留后弹出类型/签名、文档说明和声明位置（文件、行、列，均从 1 开始）。移动鼠标、滚动、输入、切换标签或窗口失去激活后关闭；注释、普通字符串、行号栏和空白处不触发。浮窗跟随深浅主题，不改变编辑器背景。
- `Alt+←` 返回、`Alt+→` 前进，最多保存 100 个位置。已打开文件的历史位置使用文本锚点，跟随编辑变化；关闭后重新打开时按行列恢复，并裁剪到实际文件范围。复用已有文档，保留未保存修改和独立撤销记录。
- 跳转目标限制为当前工程和内置语言运行时头文件；内置头文件以只读标签打开，不提供修改工具链文件的入口。
- 同步所有打开的 C/C++ 文档；启用 clangd 的内存头文件解析。依赖文件变化时刷新引用方 AST；关闭并放弃修改时先刷新该文件的磁盘索引，避免残留旧行号。取消后仍保留待刷新标记。
- 在用户语言缓存目录生成仅供分析的数据库，使用当前器件的分析参数索引尚未打开的源码及 SDK；不执行 CMake、编译器或固件构建。排除生成目录、隐藏目录、包模板和链接；自动索引上限为 5000 个源文件、10000 个目录，达到限制时记录日志。新增未打开的文件可在重新打开工程后加入索引。此数据库不是用户 CMake 的完整编译数据库，初次索引或语法/包含配置不完整时，部分实现位置可能不可用。
- 交互检查：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-navigation <output-directory> <AG32-project-directory>`。只修改内存副本，覆盖鼠标命中、右键菜单、深浅悬停浮窗、跨文件/同文件跳转、只读标准头文件、未保存头文件行号、历史与撤销、过期请求取消；不保存用户源码。

## 当前文件结构

- C/C++ 编辑区右侧显示「文件结构」，按函数、变量、类型分组，标注声明行号；类型可展开查看成员。展开函数后按需读取参数、局部变量及循环变量，避免打开文件时额外解析所有函数体。
- 单击条目或选中后按 Enter 跳转，沿用 Alt+← / Alt+→ 导航历史。顶部搜索框筛选函数、全局变量和类型；局部变量在所属函数下查看。面板宽度可拖动，减号收起，编辑器上方「文件结构」重新打开。
- 由当前工程 clangd 语义结果驱动，只显示当前文件的声明，不把引用、注释或包含的头文件内容列入。按当前宏配置解析，不显示未启用的条件编译分支；语法或包含路径不完整时结果可能不完整。
- 编辑后 450 ms 防抖刷新，包含未保存内容；切换文件、关闭工程及收起面板取消旧请求。解析在后台，返回前校验文件和文本版本，防止旧结果覆盖新文件；局部变量使用 clangd 的 AST 扩展。

## CMake 编辑提示

- `CMakeLists.txt` / `.cmake` 使用 Application 内的独立离线服务，无需启动 clangd、安装插件或执行 CMake。内置常用命令目录，按命令位置提示目标名、可见性、属性、语言标准、构建类型、布尔值等；描述、当前参数和常用示例为中文。
- 读取当前未保存文档与工程根 `CMakeLists.txt` 的声明，补全目标、变量及自定义函数/宏；`${` 中补全变量名并补齐 `}`。命令补全加括号，将光标放在括号内并继续提示参数。已有括号、变量闭合符、词中后缀不会重复插入。
- 新建工程采用 [分层 CMake](PROJECT_LAYOUT.md)：用户维护根配置，生成的 `device/CMakeLists.txt` / `device/platform.cmake` 在 IDE 中只读。提示服务同时索引这两个固定入口，能提示 `studiox_device` 与 `studiox_configure_firmware`；不要求用户把厂商配置复制回根文件。
- 支持源码、头文件目录、子目录、include 模块的路径提示。按当前目录层级读取，跳过链接与构建目录；已知源码/列表目录变量可展开为工程内目录，动态表达式不猜测。带空格的路径在未加引号时插入必要转义；不读取工程外目录。
- 输入命令、空格、`(`、`${` 或路径时自动提示；`Ctrl+Space` 手动调用，`Tab` / `Enter` 插入，`Esc` 关闭。列表底部显示命令语法及当前位置说明，`Ctrl+Shift+Space` 单独显示中文用法与示例。
- 识别行注释、带任意等号数量的括号注释/括号参数、引号与转义。注释和不展开变量的括号参数内不弹提示；中文与 UTF-16 位置转换、取消与单步撤销沿用编辑器机制。
- 范围：静态编辑辅助，未求值条件、函数作用域、生成器表达式或递归 `include`；目录变量按根工程/当前列表位置推断，不代表配置后的 CMake 模型。当前目录提示最多返回 300 项，完整提示最多 150 项。自定义模块中的全部命令、全部属性和编译器参数未做穷举；这不改变 C/C++ 服务的编译参数。
- 命令语法参考 [CMake 官方语言手册](https://cmake.org/cmake/help/v3.24/manual/cmake-language.7.html) 和 [target_include_directories](https://cmake.org/cmake/help/v3.24/command/target_include_directories.html)。目录由 `CMakeCatalog` 维护，不依赖联网帮助。
- 交互检查：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-cmake <output-directory> <project-directory>`。只使用内存副本，覆盖命令、参数、路径、变量、括号重用、Tab 插入/撤销、自动触发、注释、取消和深浅主题；检查期间停止 clangd，验证提示可独立工作。

## 外观与背景

入口：视图 → 外观与背景，或右上角齿轮、欢迎页「让工作台属于你」。

- 深色/浅色主题及自定义 JSON 配色。
- 选择本地 PNG/JPEG/BMP/GIF 图片（GIF 为静态帧）或 MP4/M4V/WMV/MOV 视频。
- 图片不超过 50 MiB，视频不超过 512 MiB；支持情况最终取决于 Windows 的媒体解码器，推荐 H.264 MP4。
- 背景不透明度、暗色遮罩、模糊、填满/完整显示均可调整并即时预览。
- 视频静音循环，最小化时暂停；可以选择窗口失去焦点时暂停。
- 「应用」保存并继续编辑，「保存并关闭」保存后退出；「取消」恢复最后一次应用的状态。

资源复制到 `%LOCALAPPDATA%/MCUStudioX/backgrounds/<sha256>.<ext>`，不会修改原文件。媒体设置独立保存为 `appearance.json`；主题仍在 `preferences.json` 中。背景层不接收鼠标输入，不改变控件或文字透明度。视频不能解码或文件丢失时显示诊断，不影响工程文件。清除背景会停播并恢复纯色工作台；缓存资源暂不自动清理。

## 本轮检查范围

多文件交互检查：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-documents <output-directory> <project-directory>`。仅复制少量文件到输出目录中的隔离工程，再检查保存/关闭/冲突、取消批量关闭、独立撤销、光标/选区/滚动、标签切换、关闭工具页和横向溢出；所有写入只发生在副本。源码与 CMake 提示另做回归检查。

基础编译和独立用户目录下的 UI 布局渲染（欢迎页、C 源码深/浅主题、头文件、CMake、外观设置）。本地临时检查覆盖编码/BOM/换行保持、磁盘修改冲突、二进制/越界拒绝、设置持久化及注释/转义字符串着色边界。没有执行 AG32 编译、烧录、硬件连接或插件端到端验证。视频播放使用 WPF MediaElement，尚未对各类系统解码器和视频编码做覆盖测试。

布局预览命令：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-ui <output-directory>`。此命令不打开工程、不连接设备、不启动构建；只在指定目录生成预览和隔离用户数据。

编辑器预览：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-editor <output-directory> <project-directory>`。只读取指定工程，不修改源码、不触发构建；输出截图和隔离用户数据。正常启动可使用 `--open <project-directory>` 直接进入工程。

提示检查：`dotnet "src/StudioX.Desktop/bin/Debug/net10.0-windows/MCU StudioX.dll" --preview-completion <output-directory> <AG32-project-directory>`。在内存副本中检查 AG32 成员/函数/变量/关键字/头文件、自动输入、Tab/Esc、撤销、词中替换、参数、注释抑制及过期请求，输出深浅主题弹窗截图，不保存项目文件、不运行固件工具。

设计参考：[CLion New UI](https://www.jetbrains.com/help/clion/new-ui.html)、[CLion 背景设置](https://www.jetbrains.com/help/clion/setting-background-image.html)；媒体控制参考 [Microsoft WPF MediaElement](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/graphics-multimedia/how-to-control-a-mediaelement-play-pause-stop-volume-and-speed)。

## 器件与模板：工程信息与新建入口

- 侧栏、工具菜单和欢迎页的“器件与模板”在工程已打开时，展示当前工程的厂商、器件、起始模板、包版本和工具集，并提供独立的“编译参数”卡片。器件信息仍只读；查看页面不会关闭工程、询问保存或重启语言服务。
- 编译参数支持工程默认、`-O0/-Og/-O1/-O2/-O3/-Os` 优化等级及工程默认、`-g0/-g2/-g3` 调试信息。点击“保存参数”后按工程保存，下次构建生效；“恢复工程默认”需保存。构建或调试期间禁用修改。窄窗口中卡片换到器件信息下方，见 [编译参数](BUILD_SETTINGS.md)。
- 信息来自工程创建记录与工程内的 `device/manifest.json`，不扫描已安装包或替换成新版包。信息缺失或版本不符时显示记录标识及说明；CubeMX 导入工程明确标注其来源，不伪造 StudioX 模板。
- 型号主标题仅显示创建记录中的芯片标识，包内型号说明（可能含频率）标注为静态信息。模板描述单列在“模板默认配置（创建时）”下，明确默认晶振与系统时钟不代表工程当前运行参数，实际配置以工程代码为准；CubeMX 显示“工程来源说明”。此页不分析用户代码或读取芯片时钟。
- “新建工程”仍使用保存／关闭确认流程。无工程时，“器件与模板”显示创建表单；关闭工程后清除旧只读信息。工程打开时导入包只增加包目录中的可用包，不改变当前配置。
- 串口终端使用接口及收发箭头图标，工程终端使用命令提示图标，构建日志使用清单及勾选图标；均为自绘矢量图标。

## 构建分析器

工程资源栏底部显示可折叠的构建分析器，沿用工作台透明背景。每个 ELF 目标显示实际构建配置、各区域的容量、链接分配量和百分比；悬停可查看精确字节、剩余空间及起始地址。80% / 95% 分别使用黄色 / 红色提示。超过容量时保留实际百分比，进度条最多填满；零容量显示横线。

区域名和容量读取 GNU ld MAP 的 `Memory Configuration` 表，包含自定义内存区，不根据型号、模板或 `.ioc` 猜测。ELF 的 PT_LOAD VMA 统计 RAM/BSS/NOLOAD/堆栈预留，LMA 的文件内容统计初始化数据在 Flash 的副本；同一区域合并实际分配区间，保留段内对齐、排除独立加载段之间的空闲空间，并避免重叠区间重复计数。不把 BSS 的加载地址当成 Flash 内容。固定在 RAM 顶部的栈不再让整个 RAM 被算满；旧统计缓存会自动重算。界面明确是上次成功构建的链接分配量，不代表运行时峰值；修改代码需要重新编译。

支持 StudioX 工程及 CubeMX 的多目标产物，MAP 须与 ELF 同名同目录。暂不支持的大端 ELF、重叠 MEMORY 别名、缺失 MAP 等显示原因，不伪造百分比、不影响固件构建成功状态。现有结果保存在 `.build/studiox-memory.json`，绑定工程记录及 ELF/MAP 长度和时间戳；打开工程只读这些明确路径，不扫描 SDK 或运行工具链。实际编译开始时清除缓存，失败/取消不显示旧结果；单独 CMake 配置保留上次成功构建统计。关闭或切换工程清空当前视图。

离线验证：`tools/StudioX.BuildMemoryValidation` 覆盖 VMA/LMA、BSS、空洞、零起始地址、独立 CCM、自定义区域、错误输入与缓存失效。`--preview-memory <new-output> <F407-project>` 在隔离副本中检查实际编译、失败、关闭和重开，不连接硬件。
